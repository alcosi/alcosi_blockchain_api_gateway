spring:
  security:
    filter:
      order: 2147483647
  data:
    redis:
      lettuce:
        pool:
          min-idle: 2
          enabled: true
common-lib:
  springdoc:
    disabled: true
  request_body_log:
    max:
      rest_template: "146800640"
webflux:
  logging:
    gateway:
      max-body-size: "146800640"
